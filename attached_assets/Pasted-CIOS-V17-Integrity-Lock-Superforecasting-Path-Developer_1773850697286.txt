CIOS — V17 Integrity Lock + Superforecasting Path
Developer / System Handoff (Use in next chat)
1. System Definition
Project: CIOS (Strategic Intelligence Engine)

Purpose:
CIOS is a structured probabilistic intelligence engine that converts signals into probability estimates and strategic guidance.

Core function:
Signals → probability update → drivers → strategic implications

CIOS is NOT:
- a dashboard
- a heuristic tool
- a generative AI system

CIOS IS:
- a deterministic signal-based probability engine
- a structured decision system
- an intelligence operating system

Public positioning:
Strategic Intelligence Engine (do not mention internal probabilistic methods)
2. Architecture (LOCKED — DO NOT REDESIGN)
CIOS consists of the following layers:

1. Question Layer (CaseHeader)
2. Signal Engine (SignalRegister + forecasting engine)
3. Field Intelligence Layer
4. Closed-Loop Deployment Layer
5. Automation Layer (candidate signal ingestion + validation)
6. Decision Output Layer

The architecture is stable.

DO NOT:
- merge layers
- introduce shortcuts between layers
- allow UI to influence core engine
3. System Design Rules (NON-NEGOTIABLE)
1. Signals must be structured
2. Signals must be validated before entering the engine
3. Probability updates must remain deterministic
4. No unstructured input may influence probability
5. Automation may propose signals but cannot update probability
6. Output layers cannot modify engine behavior
7. All forecasts must be auditable back to signals
4. Current Phase
CIOS V17 — Integrity Lock Phase

Objective:
Ensure full compliance with system design rules.

NO new features.
NO UI expansion.
NO architecture changes.

Only fix integrity violations.
5. Critical Issues to Fix (IN ORDER)
P1 — Correlated Signal Handling
Problem:
Signals are treated as independent when they are not.

Impact:
Posterior inflation (false confidence).

Required fix:
- Add correlationGroup field to signals
- Group signals during LR aggregation
- Apply dampening:
  strongest signal = full weight
  second = sqrt(LR)
  third+ = progressively reduced

- Add warning when excessive convergence detected

Do not:
- remove LR system
- introduce arbitrary caps without explanation
P2 — LR Product Stability
Problem:
LR multiplication is unbounded.

Impact:
Runaway probabilities (~0.99+ too easily)

Required fix:
- Apply log-based compression when LR product exceeds threshold
- Preserve ordering and direction

Add:
convergenceWarning flag in output
P3 — Remove Free-Text Influence
Problem:
Keyword matching in free-text fields alters probability.

Impact:
Violates structured signal rule.

Required fix:
- Remove keyword matching from:
  targetPopulation
  brand
- If retained:
  must be:
    structured
    auditable
    explicitly reported in output

Add:
keywordMatchesApplied field (if logic retained)
P4 — Signal Validation Middleware
Problem:
Invalid signals can enter engine (NaN propagation risk)

Required fix:
- Enforce schema validation at API layer
- Reject invalid signals (400 response)

Must include:
- type validation
- numeric validation
- required fields
P5 — signalType Enforcement
Problem:
Unrecognized signal types silently produce no effect

Required fix:
- Validate signalType against canonical list
- Reject invalid values
- No silent fallback to LR = 1.0
6. Implementation Constraints
DO NOT:
- change core architecture
- refactor entire engine
- introduce new modeling approaches
- modify UI except to expose warnings

DO:
- make minimal targeted fixes
- preserve canonical logic
- ensure reproducibility of outputs
7. Required Output After Each Fix

For each implemented fix, provide:

1. Files changed
2. Exact logic added/modified
3. Whether core engine logic changed (yes/no)
4. Before vs after behavior
5. Acceptance test result
6. Any risk of regression
8. Acceptance Tests (MANDATORY)

Examples:

Test 1 — Duplicate signals
- Create two similar signals
- Expect warning or dampened effect

Test 2 — Extreme signals
- Add multiple strong signals
- Ensure probability does not exceed realistic bounds
- convergenceWarning triggered

Test 3 — Invalid input
- Send malformed signal
- Expect rejection (no engine execution)

Test 4 — Invalid signalType
- Expect rejection

Test 5 — Free-text edit
- Change description text only
- Probability must NOT change
9. Next Phase (DO NOT BUILD YET)

After Integrity Lock:

Phase 2 — Calibration OS

forecast ledger

resolution tracking

Brier scoring

calibration curves

update history

Phase 3 — Superforecaster Behavior Layer

decomposition enforcement

counter-hypothesis requirement

disconfirming signals surfaced

uncertainty bands

effective signal count

update rationale tracking

10. Final Objective
CIOS must evolve into:

A superforecasting-grade strategic intelligence engine

Meaning:
- accurate over time
- calibrated
- resistant to overconfidence
- auditable
- structurally disciplined

Not:
a visually impressive tool
What YOU say in the next chat

Paste the above, then say:

Start with P4 and P5 (validation layer).

Do not proceed to other fixes until:
- validation is enforced
- signalType is enforced

Show me:
- schema
- validation logic
- example rejected request
- example valid request

Then proceed step by step.
Strategic positioning (important for you)

You now have:

CIOS → intelligence / forecasting OS

STAFFR → operational execution OS

This is not random.

This is:

Decision layer + Execution layer

That combination is what makes this system commercially serious.