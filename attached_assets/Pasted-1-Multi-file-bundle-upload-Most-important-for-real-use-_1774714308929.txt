1. Multi-file bundle upload
Most important for real use. Projects rarely come as one clean file.

2. Legacy .doc and .ppt support
Necessary for enterprise reality, especially old pharma and agency archives.

3. Stronger screenshot / image extraction
Important, but after the first two because it already partially works.

Tell Replit this:

Build all three, but prioritize them in this order:

Multi-file bundle upload
Allow users to upload multiple files in one import session
Process each file separately
Merge extracted question candidates, signals, and missing signals into one case draft
Detect contradictions across files
Show file-by-file extraction confidence
Never let one weak file fail the whole bundle
Legacy .doc and .ppt support
Add proper parsers for legacy Word and PowerPoint files so they do not fall into raw fallback
Treat these as first-class supported enterprise formats
Preserve headings, bullets, slide text, and tables as much as possible
Route parsed content through the same domain classification and signal extraction pipeline as .docx and .pptx
Stronger screenshot / image extraction
Improve extraction for OCR-heavy screenshots, charts, tables, and slide captures
Prioritize readable text recovery, table structure, headers, labels, and visible decision cues
Keep graceful fallback: even low-confidence image extraction must still produce a draft case

Implementation rules:

No import path should end in a dead end
Mixed bundles must produce one unified case draft with source attribution by file
If files disagree, surface that as a signal conflict, not an ingestion failure
Every file should receive its own extraction confidence
The final merged output should still show one proposed decision, extracted signals, missing signals, and overall ingestion confidence

Why this order:

Multi-file support solves the biggest real-world workflow gap
Legacy Office support solves the biggest format gap
Image extraction tuning is refinement on something that already works partially

One more rule to add:
Do not ask me which file is primary during import.
The system should infer that from content richness and decision relevance, then let me override only if needed.