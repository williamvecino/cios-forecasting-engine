seeing 42.4% with “No drivers identified yet” because the system is running the forecast without inputs mapped to the question.
So the engine executes, but there is nothing to compute. That’s why it looks like “nothing is analyzed.”

This is not a visualization bug.
It is a gating and mapping problem between Step 2 (Add Information) and Step 3 (See Forecast).

What Is Happening Under the Hood

The workflow currently allows:

Add Information → See Forecast

even when:

no primary drivers are linked to the question
no signals are mapped to segments
no driver weights exist
no difference logic exists for a comparative question

So the model defaults to:

Baseline probability
Low confidence
No sensitivity
Generic recommendation

That is exactly what your screen shows.

The Root Cause

The system is missing a minimum analysis readiness check before running the forecast.

Right now:

Forecast runs when the user clicks.

It should run only when:

Minimum analytical inputs exist.

The Fix

Add a Forecast Readiness Gate

This is the single most important control to implement next.

Define the Minimum Required Inputs

For any forecast to be meaningful, the system needs at least:

1 primary driver
1 supporting signal
1 segment or group
1 direction of impact

If any of these are missing:

Do not run the forecast.

Instead:

Show readiness status.

What the Screen Should Show Instead of 42.4%

Replace the probability panel with:

Forecast readiness

Not ready

Missing required inputs:

Primary driver
Supporting signal
Group-level difference
Add This Status Panel Above the Forecast

Forecast readiness

Inputs complete:

Drivers: 0
Signals: 0
Groups mapped: 0

Status:

Not ready for analysis

Behavioral Rule

IF readiness = false
THEN forecast = disabled

Button Behavior Change

Current:

Recalculate Forecast

Always clickable

Required:

Recalculate Forecast

Disabled until readiness = true

Visual Indicator

Use a simple progress meter.

Forecast readiness

██░░░░░░ 25%

Special Rule for Comparative Questions

Your question is:

Derms vs Alopecia doctors

So the system must require:

Group-specific drivers

Not general drivers.

Comparative Readiness Requirements

At least:

1 driver that differs between groups
1 signal attached to each group
1 direction of difference

Example:

Driver:

Clinical familiarity

Derms: Medium
Alopecia specialists: High

Direction:

Positive difference

Without that, comparison is impossible.

So the model returns:

No sensitive driver

Why the System Shows

Most Sensitive Driver

—

Because:

No driver variability exists.

Sensitivity analysis requires variance.

Implement This Logic

Add:

ForecastReadinessEvaluator

Pseudocode
function evaluateReadiness(case):

    if primaryDrivers.count == 0:
        return "missing drivers"

    if signals.count == 0:
        return "missing signals"

    if questionType == "comparative":
        if groupDifferences.count == 0:
            return "missing group differences"

    return "ready"
UI Behavior After Fix

Before readiness:

Probability panel hidden

Instead show:

Add signals to begin analysis

After readiness:

Probability appears

Sensitivity appears

Most sensitive driver appears

Confidence updates

One More Critical Fix

Remove this line from the current screen:

Add signals to generate a sensitivity analysis.

Replace with:

Add at least one primary driver and supporting signal to run the forecast.

That is actionable.

Why This Matters

Right now the system:

looks like it is analyzing
but is not

After the fix:

The system will clearly signal:

ready
or
not ready

This eliminates confusion immediately.

Bottom Line

Nothing is analyzed because the system has:

no mapped drivers
no signal variance
no group differences

Add a readiness gate that blocks forecasting until minimum analytical inputs exist.