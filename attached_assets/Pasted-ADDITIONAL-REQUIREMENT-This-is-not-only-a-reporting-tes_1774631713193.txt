ADDITIONAL REQUIREMENT

This is not only a reporting test.

After running the 10-question workflow test suite, identify every failed test and fix the underlying issue until all tests pass or until the remaining failures are blocked by a clearly stated architectural dependency.

WORKING RULE

Do not stop at producing test results.
Use the test results as an active debugging and repair loop.

REQUIRED PROCESS

1. Run the full workflow test suite
2. Record all failures
3. For each failure:
   - identify the exact failure point
   - classify the root cause
   - implement the smallest correct fix
   - rerun the affected test
4. After fixing individual failures, rerun the full 10-question suite
5. Repeat until:
   - all tests pass
   OR
   - remaining failures are explicitly documented as blocked by larger missing architecture

ROOT CAUSE CATEGORIES

Use these categories when diagnosing failures:

- question parsing failure
- field extraction failure
- question-type misclassification
- context binding failure
- signal relevance failure
- driver mapping failure
- forecast readiness failure
- forecast calculation failure
- sensitivity analysis failure
- decision output failure
- UI-to-logic wiring failure

FIXING RULES

1. Prefer the smallest targeted fix that solves the actual root cause
2. Do not hardcode answers only to make the 10 questions pass
3. Fix reusable logic, not just the single example
4. Preserve the current architecture unless a structural issue is clearly required
5. If a failure comes from missing architecture, document it clearly instead of masking it

REQUIRED OUTPUTS

After the repair loop, provide 3 outputs:

1. Final test results
Save to:
   /tests/workflow_test_results.json

2. Fix log
Save to:
   /tests/workflow_fix_log.json

Each fix log entry must include:
- question
- failure
- root cause category
- fix implemented
- files changed
- retest result

3. Remaining blockers report
Save to:
   /tests/workflow_blockers.md

Only include items that could not be fixed in this round.
For each blocker include:
- failed question
- exact failure
- why it could not be fixed cleanly
- what architectural addition is needed

SUCCESS STANDARD

The task is successful only if:
- the system attempts to repair failures, not just report them
- fixes are applied to reusable logic
- the full suite is rerun after repairs
- results clearly show what now passes and what still fails

IMPORTANT

Do not stop after the first round of failures.
Run the test → fix → retest loop automatically.

FINAL DELIVERABLE

At the end, summarize:
- how many tests passed initially
- how many pass after fixes
- which failures remain
- which files were changed most materially