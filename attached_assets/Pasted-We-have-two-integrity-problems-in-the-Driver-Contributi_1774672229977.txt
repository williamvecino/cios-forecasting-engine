We have two integrity problems in the Driver Contribution Breakdown.

==================================================
PROBLEM 1 — CONTRIBUTION ACCOUNTING MISMATCH
==================================================

Current display:

Upward: +25 pts
Downward: -5 pts
Net: +20 pts
Shift: +25 pts

This is mathematically inconsistent.

The system moved the probability by +25 points but only explains +20.

REQUIRED RULE

Probability shift must equal net contributions.

Prior probability
+ Net contributions
= Final probability

If mismatch exists, do not display the result as valid.

==================================================
FIX

Recalculate contribution totals after actor adjustments and rounding.

Or:

Add a reconciliation line:

Unattributed adjustment: +5 pts

But do not allow unexplained movement.

==================================================
PROBLEM 2 — CONTRADICTORY SIGNALS REMAIN ACTIVE
==================================================

User added signals:

prior authorization not needed
payer authorization no needed
payer just included in formulary

These directly contradict older signals:

prior authorization required
payer friction observed

The system currently keeps both signals active and averages them.

This creates artificial constraint and prevents gate improvement.

==================================================
REQUIRED BEHAVIOR

When a new signal contradicts an older signal:

Detect contradiction.

Then:

Mark the older signal as superseded.

Do not average opposing signals silently.

==================================================
IMPLEMENTATION

Add conflict detection:

If:

prior authorization required

and later:

prior authorization not required

Then:

Set:

status = superseded

Keep for history
Exclude from active contribution calculation

==================================================
REQUIRED UI

Display:

Superseded signal
Replaced by newer evidence

==================================================
VALIDATION RULE

A resolved reimbursement barrier must not remain constrained by outdated signals.

==================================================
SUCCESS CONDITION

The system is correct only if:

Net contributions equal probability shift

AND

Contradictory signals are resolved deterministically
not averaged silently