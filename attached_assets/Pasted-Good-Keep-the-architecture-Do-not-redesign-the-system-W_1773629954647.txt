Good. Keep the architecture. Do not redesign the system.

We now need to move CIOS from:
advanced forecasting system with early learning capability
to
adaptive intelligence engine.

Build the next phase in three focused upgrades, in this order.

UPGRADE 1 — INTEGRATE AGENT SIMULATION INTO THE FORECAST

Current issue:
There are two parallel actor systems:
1. static Bayesian actor weight table used in the posterior calculation
2. dynamic multi-agent simulation that is advisory only

This must be unified.

Build:
- replace or blend the static actor translation factor with the agent-derived actor simulation score
- the final posterior must now be influenced by:
  - academic KOL influence
  - community specialist response
  - payer drag
  - competitor dampening
  - hospital / site-of-care dynamics where relevant
- actor-to-actor influence must materially affect the forecast number, not just the commentary

Requirements:
- preserve explainability
- show how the actor contribution changed the final outlook
- clearly display:
  - actor base stance
  - cross-agent influence
  - final actor contribution to forecast

Do not make the actor layer decorative. It must change the computed outcome.

UPGRADE 2 — EXPAND AND OPERATIONALIZE THE ANALOG LIBRARY

Current issue:
The analog retrieval engine is structurally sound but the case library is too thin to provide clinically meaningful analogs.

Build:
- expand the analog library from 5 placeholder cases to at least 30 calibrated cases
- include 3–5 cases per major therapy area, with urgent priority for:
  - CNS / psychiatry
  - cardiology
  - oncology
  - immunology / dermatology
  - infectious disease
  - rare disease

For each analog case, store:
- asset name
- modality / formulation
- therapy area
- disease
- specialty
- launch stage
- core signal mix
- key adoption barrier
- key adoption enabler
- actor pattern
- observed adoption trajectory
- final outcome summary

Requirements:
- retrieved analogs must influence forecast interpretation and scenario framing
- show why the analog matters
- show key similarities
- show key differences
- show what lesson the active case should take from the analog

Do not just display analogs. Make them strategically useful.

UPGRADE 3 — CLOSE THE CALIBRATION LEARNING LOOP

Current issue:
Calibration records errors but does not yet improve the model.

Build:
- when enough calibrated cases accumulate, use observed forecast error to adjust signal-type LR coefficients and / or actor contribution weights
- create rules such as:
  - if a signal type shows persistent overestimation or underestimation across a minimum number of cases, apply a correction factor
- implement freshness decay so older signals contribute less over time
- make this adjustable by signal type:
  - clinical evidence decays slowly
  - field intelligence decays faster
  - competitive and access signals decay at moderate speed

Requirements:
- preserve auditability
- show where calibration adjustments came from
- do not silently change parameters
- log all parameter updates and the reason for each update

IMPORTANT DESIGN RULES

1. Do not redesign the app.
2. Keep the current architecture and improve the wiring.
3. Preserve disease-agnostic behavior.
4. Preserve standalone mode while remaining integration-ready for MIOS and OHOS.
5. Keep the system explainable and strategic, not technical or opaque.
6. Do not turn this into a generic chatbot.

OUTPUT I WANT BEFORE YOU BUILD

Before implementation, show me:
1. the architecture changes for each of the 3 upgrades
2. the data model changes required
3. the exact modules / files you will modify
4. how the new actor-integrated forecast will work
5. how analog cases will now influence interpretation
6. how calibration will adjust the model over time
7. any assumptions you are making

Then proceed with implementation in this order:
1. agent integration
2. analog library expansion
3. calibration feedback + signal decay

After implementation, run a diagnostic and tell me:
- what changed
- whether the forecast number now uses the dynamic agent model
- whether analogs are now clinically meaningful
- whether calibration now changes future forecasts