The new workflow shell was not actually applied.

What I’m still seeing is the legacy app shell and legacy sidebar/navigation. This is a structural mounting issue, not a styling issue.

Evidence:
The UI still shows the old left-nav architecture with these items:

* Questions
* Adopter Discovery
* Event Radar
* Signal Detection
* Signal Validation
* Dashboard
* Forecast Ledger
* Calibration
* System Map

That means the old shared layout/sidebar component is still mounted around the app.

What I need you to do:

1. Find the component that renders the current left sidebar
   Search the codebase for these exact visible labels:

* "Adopter Discovery"
* "Event Radar"
* "Signal Detection"
* "Signal Validation"
* "Forecast Ledger"
* "Calibration"
* "System Map"

The file containing those labels is the legacy navigation shell still controlling the UI.

2. Replace the legacy app shell with the new workflow shell
   The app should no longer expose system architecture in the main navigation.

The left nav must be replaced with only:

* 1 — Ask a Question
* 2 — Add Information
* 3 — See Forecast
* 4 — Decide
* Advanced / System

3. Remove these items from the main nav completely
   These should NOT appear as first-order navigation items anymore:

* Adopter Discovery
* Event Radar
* Signal Detection
* Signal Validation
* Forecast Ledger
* Calibration
* System Map

4. Re-home those modules correctly
   New structure:

Primary workflow:

* /question → Ask a Question
* /signals → Add Information
* /forecast → See Forecast
* /decide → Decide

Advanced / System drawer:

* Forecast Ledger
* Calibration
* System Map
* Workbench
* Stability Tests

5. Enforce question-first gating
   Signals, Forecast, and Decide must remain locked or redirected to /question unless an active question exists.

6. Do not just add new pages
   Do not leave the old sidebar in place and layer new pages underneath it.
   The root/shared layout itself must be replaced.

What I want back from you:

* the exact file currently rendering the old sidebar
* confirmation that it has been replaced or bypassed
* the exact root layout file now mounting WorkflowLayout
* confirmation that the main nav now only shows the 4-step flow plus Advanced/System

Success criteria:
If I still see section headers like:

* Decision
* Evidence
* Prediction
* Learning

then the old shell is still active.

After the fix, the left nav should show only:

* Ask a Question
* Add Information
* See Forecast
* Decide
* Advanced / System
