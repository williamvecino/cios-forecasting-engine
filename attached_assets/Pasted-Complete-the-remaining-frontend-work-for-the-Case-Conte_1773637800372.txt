Complete the remaining frontend work for the Case Context Integrity phase.

Do not change backend logic unless strictly required for wiring.
The API layer is already verified.
This is a frontend completion pass only.

Build these 3 missing UI pieces:

1. COVERAGE MAP SECTION ON CALIBRATION PAGE
Add a new Coverage Map section to calibration/index.tsx.

Requirements:
- consume GET /calibration/coverage-map
- render therapy-area rows
- render question-type rows
- render global row
- columns must be the probability buckets:
  - 0.40-0.60
  - 0.60-0.75
  - 0.75-0.90
  - 0.90+
- each cell must show:
  - n
  - maturity
  - correction active/inactive
  - low-sample warning if present
  - thresholdMet state
- use simple color-coded maturity states:
  - none
  - low
  - medium
  - high
- keep it structured and inspectable, not decorative

2. VALIDATION SECTION ENRICHMENT
In the existing Validation Report section on calibration/index.tsx, add:
- questionTypeBreakout rendering
- segmented verdict banner
- mixed-behavior indicator if mixedBehaviorDetected = true

Requirements:
- preserve current therapy-area breakout and per-case table
- add a separate question-type breakout block
- surface:
  - segmentedVerdict
  - mixedBehaviorDetected
- make the verdict visible near the top of the validation section
- if mixedBehaviorDetected = true, show a clear caution banner

3. FORECAST TRACE CARD ENRICHMENT
In cases/forecast.tsx, update the Forecast Trace card to consume traceData.caseContext.

Add a dedicated Case Context row/block showing:
- therapeuticArea
- diseaseState
- specialty
- questionType
- caseMode
- timeHorizon

Requirements:
- place Case Context near the top of the expanded trace card
- keep coverageNote separate as its own banner
- do not bury coverageNote under Analog Support
- preserve current calibration summary and driver sections

ACCEPTANCE CRITERIA
I want proof of the following after build:
1. screenshot or equivalent render proof of Coverage Map section
2. screenshot or equivalent render proof of question-type breakout + segmented verdict
3. screenshot or equivalent render proof of Case Context row in Forecast Trace
4. confirmation that no backend endpoints were changed unless necessary for wiring

Do not add new features.
Do not rewrite existing components unnecessarily.
Finish the current phase cleanly.